{
  "name": "lightory-server",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "allure-vitest": "^3.10.2",
    "vitest": "^3.2.1"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/static": "^9.1.1",
    "@fastify/websocket": "^11.3.0",
    "fastify": "^5.10.0"
  }
}
